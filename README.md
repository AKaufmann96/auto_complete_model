# Система автодополнения текста

Данный проект реализует систему автодополнения текста на основе нейросетевых моделей.  
Цель — сравнить эффективность двух архитектур:
- **LSTM-модель**, обученная с нуля на внутренних текстах;
- **Предобученный трансформер DistilGPT-2**, применённый в режиме zero-shot.

Оценка производится по метрикам **ROUGE** (ROUGE-1, ROUGE-2, ROUGE-L) на задаче генерации продолжения фразы на основе первых 75% исходного текста.

Результаты позволяют сформулировать рекомендации по выбору архитектуры для внедрения в продакшн-среду.

---

## 📁 Структура проекта

auto_complete_model/
├── data/                            # Датасеты
│   ├── raw_dataset.csv              # Сырой датасет
│   ├── dataset_processed.csv        # Очищенный датасет
│   ├── train.csv                    # Обучающая выборка
│   ├── val.csv                      # Валидационная выборка
│   └── test.csv                     # Тестовая выборка
│
├── src/                             # Исходный код
│   ├── data_utils.py                # Очистка и разбиение данных
│   ├── next_token_dataset.py        # PyTorch Dataset и DataLoader
│   ├── lstm_model.py                # Архитектура LSTM
│   ├── lstm_train.py                # Обучение LSTM
│   ├── eval_lstm.py                 # Оценка LSTM (ROUGE, генерация)
│   └── eval_transformer_pipeline.py # Оценка DistilGPT-2
│
├── models/                          # Веса моделей и словарь
│   └── lstm_model.pth               # Сохранённая LSTM-модель
│   └── vocab.pkl                    # Словарь токенов
│
├── results/                         # Результаты генерации
│   └── transformer_val_results.csv  # Примеры от DistilGPT-2
│
├── configs/                         # Конфигурации (при необходимости)
│
├── solution.ipynb                   # Ноутбук с полным решением
│
└── requirements.txt                 # Зависимости


---

## ⚙️ Настройка окружения

### 1. Создание виртуального окружения

Рекомендуется использовать `venv` для изоляции зависимостей:

python -m venv venv

### 2. Активация виртуального окружения

Linux / macOS:
source venv/bin/activate

### Установка зависимостей

Убедитесь, что Вы находитесь в корне проекта и активировали виртуальное окружение.

Установите зависимости из requirements.txt:

pip install -r requirements.txt

### Запуск проекта

Запуск через Jupyter Notebook (рекомендуется):

pip install jupyter
jupyter notebook solution.ipynb

---

## 🧩 Назначение модулей

- data_utils.py -	Очистка текста, разбиение на train/val/test

- next_token_dataset.py -	PyTorch Dataset, построение словаря, формирование пар (последовательность → следующий токен)

- lstm_model.py	Архитектура LSTM с эмбеддингами, скрытыми состояниями и механизмом генерации

- lstm_train.py	Обучение модели: цикл, лосс, оптимизатор, сохранение лучшей версии

- eval_lstm.py	Оценка качества: accuracy, ROUGE, примеры генерации

- eval_transformer_pipeline.py	Инференс DistilGPT-2, генерация, сравнение с LSTM по ROUGE

---

## Результаты

После выполнения:

Метрики ROUGE для обеих моделей будут выведены в консоль.
Примеры генерации сохраняются в results/.
Сравнение моделей доступно в solution.ipynb.

---

## 📈 Методология оценки
Целевая задача: предсказать оставшиеся 25% текста по первым 75%.

Метрики:

- ROUGE-1, ROUGE-2, ROUGE-L — F1-мера совпадения n-грамм.

- Accuracy — доля точного совпадения предсказанного токена.

Сравнение:

- LSTM: обучена на домене, легковесна.
- DistilGPT-2: предобучена, не дообучалась, используется в zero-shot режиме.